# Checkpoint

- p0: Akinator sample workspace is now mirrored in-repo at `demo/targets/akinator/`, with APK splits, `jadx-out`, runtime evidence, and the imported `libpairipcore` native analysis tree kept together for later hardening work. Future work should use the in-repo sample root as the primary path instead of the old desktop-only workspace.
- p0: Current Akinator conclusion is `Android static environment detection first`, not `Frida detection first`. Confirmed startup-root surfaces are constructor-time `ro.arch` / `AT_HWCAP` / `AT_HWCAP2` profiling, JNI_OnLoad-root module/phdr walk in `sub_17E1C`, dynamic libc/symbol probing in `sub_2C5F0`, and filesystem probing in `sub_2C934`. These are enough to classify the sample as static environment and generic integrity inspection, but not yet enough to call the startup path Frida-specific.
- p0: Fresh runtime validation with upstream `frida 17.8.0` on the connected Pixel 8a did not produce a stable `20s` no-change hold on Akinator. `attach` kept the original pid alive for most of the window but the process disappeared at `t≈19.991s`, after which the app relaunched on a new pid; `spawn` died almost immediately, detached with `reason=process-terminated`, and crashed at the already known `libpairipcore.so+0x5d75c` site (`SIGSEGV/SEGV_ACCERR`). Evidence is retained under `demo/targets/akinator/evidence/2026-04-05-160418-frida-20s-hold/`.
- p0: A naked control run is stable for `25s`, so the delayed `attach` death is Frida-induced, not a normal app timeout. A second `late attach` run after `10s` of naked execution changes the phenotype again: instead of the earlier `WebView + memfd:sunda-agent-64.so + SIGABRT` path, the app later dies with `SIGSEGV/SEGV_ACCERR` in `libpairipcore.so+0x4746c` on `Thread-2` and restarts on a new pid. Evidence is retained under `demo/targets/akinator/evidence/2026-04-05-161031-baseline-25s-hold/` and `demo/targets/akinator/evidence/2026-04-05-161146-late-attach-10s-then-20s/`.
- p0: Current runtime review now treats Akinator as a multi-phenotype sample, but the current stable picture is narrower than before. On the current device state: naked baseline is stable for `25s`, plain attach is also stable for `25s` both with and without a no-op script, `spawn` still reproduces the startup verifier crash at `0x5d75c`, and `late attach` still drives a later native crash in `sub_2EED4` at `0x4746c`. The consolidated review is in `demo/targets/akinator/analysis/runtime-evidence-review.md`.
- p1: Direct Frida instruction hooks on `0x47424` / `0x4746c` proved that the late-attach branch really executes inside `sub_2EED4` on `Thread-2`, but the probe perturbs the phenotype back into a `std::bad_alloc` WebView-side abort. Treat direct Frida probes there as confirmation of reachability only, not as low-perturbation ground truth.
- p0: `sub_2EED4` is now upgraded from a generic suspicious dispatcher to a confirmed VM-controlled late-attach branch. `pairip_vm_interpreter_core_tentative` reaches it through dispatcher block `0x533d0`, which passes a caller-built state object at `sp+0xa8`, a continuation/output slot at `sp+0xa4`, and selector `w23`. The local `0x47424..0x474c4` region is best treated as a blob-verification loop whose crash site is `0x4746c`. The current focused note is in `demo/targets/akinator/analysis/sub2eed4-late-attach-branch.md`.
- p1: A low-count entry probe on `sub_2EED4` now shows that the caller-built object passed at `sp+0xa8` stays structurally stable across calls, while `sp+0xa4` is a mutable 32-bit side slot immediately before it. Observed selectors at entry include `0x24`, `0x9f`, `0x19`, and `0x0e`. This makes the next recovery target concrete: decode the `x0` descriptor layout and the meaning of the mutable `x2` side slot instead of probing deeper instructions.
- p0: The current task-level summary is now consolidated in `demo/targets/akinator/analysis/final-vm-and-crash-summary.md`. The stable bottom line is: the startup asset runs inside a native VM, the VM already has two recovered verifier families (`0x5d75c` startup branch and `sub_2EED4 -> 0x4746c` late-attach branch), and Frida timing determines which runtime phenotype is exercised.
- p0: Current pending classification is now explicit in `demo/targets/akinator/analysis/pending-classification.md`: the stable hit points `0x5d75c` and `sub_2EED4 -> 0x4746c` are both treated as sample-side detection points, while no current stable server bug crash is proven. The old WebView or `sunda-agent-64.so` abort is downgraded to a historical or probe-perturbed path because plain attach is now stable for `25s`.
- p1: The remaining Akinator gap is the dataflow link from environment producers into the startup VM verifier family `0x5d61c..0x5d798` for asset `ReqL9SxJHgWgm3cn`. The next slice should prove which producer among `sub_17E1C`, `sub_2C5F0`, `sub_2C934`, `sub_2EED4`, and `sub_65ADC` feeds the blob base, expected value, or selector consumed by that handler family.
- p0: Active baseline is `frida17.8.0/` plus local `frida-glib/`. The current `main` worktree is clean, the Android ARM64 `sunda` server rebuilds successfully, and the rebuilt macOS `17.8.0` host tools still interoperate with the connected Pixel 8a.
- p0: Demo validation is currently green. `demo/tools/run_feature_tests.py` passes the seven-device-check baseline, and `demo/tools/run_pipeline.py` reports `baseline=0` and `injected=0` for both attach and spawn with the current rebuilt host/server pair.
- p1: The next hardening pass should stay on non-contract fingerprints only. Current remaining live surfaces are `symbolic_brand`, small `other_literal_or_path` residues such as `pool-frida`, and `source_debug_path`; already-cleared buckets like `source_subprojects_path`, `runtime_asset_path`, and `rpc_channel` should stay closed.
- p0: The current demo canary path itself no longer exposes remaining `frida` strings on the stable spawn validation route. `python3 demo/tools/run_pipeline.py spawn --iterations 1` still reports `baseline=0` and `injected=0`, so there is no demo-self remaining-string cleanup to do right now. If fingerprint cleanup should continue, the next slice must target another injected app/process view rather than the demo app's own process.
- p1: Thread-name hardening is currently gated off by live evidence. Looking at `/proc/self/task/*/comm` on the injected target process itself for both the demo app and `com.android.purebilibili`, there were no visible `gmain`, `Saturday`, `Thursday`, `pool-spawner`, or similar GLib-default thread names. Only app/system names surfaced, such as binder threads, ART daemons, RenderThread, OkHttp/Firebase workers, and app-owned `pool-*` threads. Do not rename GLib thread names until they appear from the demo-target process view.
- p1: Do not rename `protocol_namespace` unilaterally. Any `re.frida.*` or `/re/frida/*` migration still requires coordinated host, server, and embedded-agent changes; single-sided rewrites have already broken `frida-ps -U` and spawn-holder resolution at `"/re/frida/AgentSessionProvider"`.
- p1: Host rebuild discipline is still an active constraint. `frida-python` consumes `subprojects/frida-python/subprojects/frida-core` instead of only the top-level `subprojects/frida-core`, and local source rebuilds still depend on `SSL_CERT_FILE`, `MACOS_CERTID=-`, and pinned macOS toolchain variables.
- p1: The `com.android.purebilibili` Java hook blocker is now narrowed to an Android 16 ART layout/semantic mismatch inside `frida-java-bridge`, not to Sunda transport. On-device repro through the real `frida-tools` bridge path still fails on both attach and spawn at `Unable to find copied methods in java/lang/Thread; please file a bug`; lldb confirmed the `java/lang/Thread` class object's `methods_` array length is `125`, while the class body exposes no nearby `u16/u32 == 125` candidate for the old heuristic. AOSP ART source matches that direction: `copied_methods_offset_` is the start index of copied methods, not the total method count.
- p0: The Android 16 Java bridge hotfix is now landed in tracked repo paths by explicitly reviewing only `frida17.8.0/subprojects/frida-tools/bridges/node_modules/frida-java-bridge/lib/android.js` and `lib/class-model.js`, with `.gitignore` narrowed so those two sources remain reviewable without opening the rest of `subprojects/`. The patch documents why Android 16 needs the fallback and why ART raw flags are no longer sufficient for Java modifiers.
- p0: Patch verification is green. Rebuilding a temporary bridge bundle from the checked-in patched sources restores the broader Java suite on both attach and spawn for `com.android.purebilibili`: static field access, static method resolution/call, overload selection, app-context access, `Java.enumerateMethods`, `Java.choose`, static hook replacement, constructor hook replacement, and instance hook replacement on both `Thread.getName()` and `Application.getPackageName()`. The existing `demo/tools/run_feature_tests.py` device baseline also remains green (`allPassed=true`).
- p2: Known non-blockers remain external or parity issues. Spawn plus attach against `/system/bin/sleep` still reproduces the same `/proc/self/fd/3` failure on official `frida-server` `17.8.0`.
- p2: Repo docs now lag the checkpoint. `README.md` should be refreshed later so it matches the current clean validation baseline and the current protocol-migration constraint instead of the older transitional wording.
