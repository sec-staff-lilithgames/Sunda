# Checkpoint

- p0: Active baseline is `frida17.8.0/` plus local `frida-glib/`. The current `main` worktree is clean, the Android ARM64 `sunda` server rebuilds successfully, and the rebuilt macOS `17.8.0` host tools still interoperate with the connected Pixel 8a.
- p0: Demo validation is currently green. `demo/tools/run_feature_tests.py` passes the seven-device-check baseline, and `demo/tools/run_pipeline.py` reports `baseline=0` and `injected=0` for both attach and spawn with the current rebuilt host/server pair.
- p1: The next hardening pass should stay on non-contract fingerprints only. Current remaining live surfaces are `symbolic_brand`, small `other_literal_or_path` residues such as `pool-frida`, and `source_debug_path`; already-cleared buckets like `source_subprojects_path`, `runtime_asset_path`, and `rpc_channel` should stay closed.
- p1: Do not rename `protocol_namespace` unilaterally. Any `re.frida.*` or `/re/frida/*` migration still requires coordinated host, server, and embedded-agent changes; single-sided rewrites have already broken `frida-ps -U` and spawn-holder resolution at `"/re/frida/AgentSessionProvider"`.
- p1: Host rebuild discipline is still an active constraint. `frida-python` consumes `subprojects/frida-python/subprojects/frida-core` instead of only the top-level `subprojects/frida-core`, and local source rebuilds still depend on `SSL_CERT_FILE`, `MACOS_CERTID=-`, and pinned macOS toolchain variables.
- p1: The `com.android.purebilibili` Java hook blocker is now narrowed to an Android 16 ART layout/semantic mismatch inside `frida-java-bridge`, not to Sunda transport. On-device repro through the real `frida-tools` bridge path still fails on both attach and spawn at `Unable to find copied methods in java/lang/Thread; please file a bug`; lldb confirmed the `java/lang/Thread` class object's `methods_` array length is `125`, while the class body exposes no nearby `u16/u32 == 125` candidate for the old heuristic. AOSP ART source matches that direction: `copied_methods_offset_` is the start index of copied methods, not the total method count.
- p0: The Android 16 Java bridge hotfix is now landed in tracked repo paths by explicitly reviewing only `frida17.8.0/subprojects/frida-tools/bridges/node_modules/frida-java-bridge/lib/android.js` and `lib/class-model.js`, with `.gitignore` narrowed so those two sources remain reviewable without opening the rest of `subprojects/`. The patch documents why Android 16 needs the fallback and why ART raw flags are no longer sufficient for Java modifiers.
- p0: Patch verification is green. Rebuilding a temporary bridge bundle from the checked-in patched sources restores the broader Java suite on both attach and spawn for `com.android.purebilibili`: static field access, static method resolution/call, overload selection, app-context access, `Java.enumerateMethods`, `Java.choose`, static hook replacement, constructor hook replacement, and instance hook replacement on both `Thread.getName()` and `Application.getPackageName()`. The existing `demo/tools/run_feature_tests.py` device baseline also remains green (`allPassed=true`).
- p2: Known non-blockers remain external or parity issues. Spawn plus attach against `/system/bin/sleep` still reproduces the same `/proc/self/fd/3` failure on official `frida-server` `17.8.0`.
- p2: Repo docs now lag the checkpoint. `README.md` should be refreshed later so it matches the current clean validation baseline and the current protocol-migration constraint instead of the older transitional wording.
