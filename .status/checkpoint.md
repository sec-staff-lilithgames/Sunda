# Checkpoint

- p0: `frida17.8.0/` is now the active working tree for the new route, and the top-level directory name now matches the actual Frida `17.8.0` baseline carried by the repo.
- p0: `frida-glib/` remains a local preserved component. The current build still depends on local GLib priority logic in `releng/env.py` and `releng/meson_configure.py`, including the Android `target-pkgconfig` overlay path and `gio-2.0.pc` handling.
- p0: A local Android ARM64 build from the migrated tree is functional. With `SSL_CERT_FILE=/Users/siberia/Library/Python/3.12/lib/python/site-packages/certifi/cacert.pem` and `ANDROID_NDK_ROOT=/Users/siberia/Library/Android/sdk/ndk/25.2.9519653`, the server binary at `build-android-arm64-server-cert/subprojects/frida-core/server/sunda` builds successfully, starts on the connected Pixel 8a, and passes `frida-ps -U`.
- p0: The non-protocol runtime cleanup pass is now landed for the Android helper/compiler surface. The rebuilt server now carries `sunda-helper-*`, `Usage: sunda-helper`, `/re/sunday/Helper`, `re.sunday.Helper`, `re/sunday/HelperBackend`, `sunda:rpc`, `/sunda/runtime/*`, and `/sunda/`. It no longer exposes the old `/data/local/tmp/frida-helper-`, `/frida-helper-`, `Usage: frida-helper`, `frida:rpc`, or `/frida/runtime/*` strings in that layer.
- p0: The demo scanner now emits self-explaining reports. `latest.json` and the app summary include per-hit `matchText` / `bucket` / `why`, plus summary-level `bucketDetails` and `topModules`, and the host pipeline now surfaces those buckets directly in attach/spawn failure output. The explainability pass was also hardened to avoid self-poisoning the baseline with new in-app `frida` literals.
- p0: Latest demo-side validation still confirms why app perspective matters more than `adb shell` perspective. With the root-run `sunda-built-frida-clean` server, the current rerun reported:
  calibration before=2 after=0;
  manual injected final: totalHits=177.
  The dominant hit source is still `/memfd:sunda-agent-64.so (deleted)` with 139 hits, so the remaining fingerprint surface is now concentrated inside the injected agent rather than helper/runtime glue.
- p0: A build-level path remap pass is now landed for `frida-core` and `frida-gum`. The rebuilt embedded `sunda-agent-64.so` no longer carries `../subprojects/frida-*` style source paths; they now map to neutral `/sunda-src/...` prefixes. On the latest manual injected scan after deployment, total hits dropped from 219 to 191 and the entire `source_subprojects_path` bucket disappeared.
- p0: A follow-up literal cleanup pass is now landed in `src/system.vala`, `lib/payload/portal-client.vala`, `src/linux/frida-helper-process.vala`, `src/linux/frida-helper-backend.vala`, `lib/base/session.vala`, `lib/payload/base-agent-session.vala`, `src/barebone/barebone-host-session.vala`, `lib/gadget/gadget.vala`, `lib/agent/agent.vala`, `lib/base/socket.vala`, `gumjs/gumcmodule.c`, and `src/embed-agent.py`. After rebuild and redeploy, total hits dropped further from 191 to 177, and `other_literal_or_path` dropped from 11 to 3.
- p1: The latest demo hit buckets on injected scans are now clear enough to drive the next hardening pass. Current counts are effectively stable between attach/spawn:
  `symbolic_brand`: 104 hits, e.g. `Frida.VirtualOutputStream.close_async`;
  `protocol_namespace`: 64 hits, e.g. `/re/frida/AgentSessionProvider`;
  `source_subprojects_path`: no longer active after the path remap pass;
  `runtime_asset_path` and `rpc_channel`: not present in the latest manual injected rerun, so they are not current active buckets;
  `other_literal_or_path`: now only 3 hits, specifically two generic `frida` literals and one `pool-frida` thread name;
  `source_debug_path`: still about 6 hits, e.g. `/__w/frida/frida/deps/src/_sdk.out/...`.
- p1: `protocol_namespace` is not currently a safe single-sided erase target. A server-only rename of `lib/base/session.vala` from `re.frida.*` / `/re/frida/*` to `re.sunday.*` / `/re/sunday/*` immediately broke the current host 17.4.1 client chain (`frida-ps -U` failed until reverted). Keep protocol namespace unchanged for now if the goal is to preserve the live demo-driven validation loop; prioritize buckets that do not require synchronized host/client migration.
- p1: The next safe cleanup target is now effectively just `pool-frida`, plus any future generic `frida` fragments that can be shown to be non-contract and non-brand-critical. The remaining `protocol_namespace` bucket is still explicitly out of scope for unilateral server-side rename while the current 17.4.1 host validation chain is in use.
- p1: A newer runtime proof now strengthens the same protocol constraint. Rewriting only the embedded agent blob from `re.frida.*` / `/re/frida/*` to `re.sunda.*` / `/re/sunda/*` let the server boot, but the demo spawn holder failed with `frida.ProtocolError: object does not exist at path "/re/frida/AgentSessionProvider"`. This confirms the current host Python/CLI stack still depends on the old protocol namespace even when the server itself remains reachable.
- p1: The current in-progress hardening pass keeps only non-contract agent rewrites in `subprojects/frida-core/src/embed-agent.py`: brand-facing `Frida.*` strings now map to `Sunda.*`, the upstream debug-path prefix `/__w/frida/frida/` now maps to `/sunda-src/upstr/`, and `remote Frida` now maps to `remote Sunda`. These changes survive local rebuild and do not by themselves prove injected demo parity yet.
- p1: Latest live validation on the rebuilt root-run `sunda` shows the app baseline has reached `0 hit` again for both attach/spawn baseline scans (`latest.json` reason `attach_baseline_0` and `spawn_baseline_0`). However the injected phase is not yet re-verified on the current experimental server: the demo holder still exits before producing an injected `latest.json`, and logcat shows the app being force-stopped and relaunched during the spawn loop.
- p0: The macOS host toolchain is no longer using the prebuilt PyPI `17.4.1` pair. `/Library/Frameworks/Python.framework/Versions/3.12/bin/frida-ps` now reports `17.8.0`, `python3 -m pip show frida frida-tools` shows both packages installed at `17.8.0`, and `frida-ps -U` still enumerates the connected device.
- p1: Rebuilding the macOS host packages from source requires local environment fixes. `frida-python` needed `SSL_CERT_FILE=/Users/siberia/Library/Python/3.12/lib/python/site-packages/certifi/cacert.pem` to fetch host SDK artifacts and `MACOS_CERTID=-` for ad-hoc signing of the helper; `frida-tools` needed `CC`, `CXX`, and `SDKROOT` pinned to the Xcode toolchain so meson-python would accept the compiler on macOS. Installing the resulting local wheels with `pip --no-deps` avoided pulling in a prebuilt upstream `frida` wheel.
- p0: The shared runtime protocol definition in `subprojects/frida-core/lib/base/session.vala` is now renamed from `re.frida.*` / `/re/frida/*` to `re.sunday.*` / `/re/sunday/*`, and the host Python/tools message channel now uses `sunda:rpc`, `sunda:load-bridge`, `sunda:bridge-loaded`, and `sunda:cmodule-payload`. The default control port in `subprojects/frida-core/lib/base/socket.vala` is now `27142` instead of `27042`.
- p1: Secondary host/runtime identifiers were also migrated to the new namespace in the active source trees: `re.sunday.Gadget`, `re.sunday.Helper`, `re.sunday.Server`, `re.sunday.policyd`, plus the Android unix socket prefix `sunda:`. The relevant darwin/windows helper types, bundle identifiers, and CLI help text were updated in the vendored source trees to match.
- p1: Coordinated rebuild and redeploy of both sides succeeded far enough for the main USB path: the source-built macOS host `17.8.0` tools still enumerate the device with `frida-ps -U`, and the rebuilt Android `sunda` server starts and stays alive after deployment.
- p1: A new build-system constraint is now explicit on the host side. The `frida-python` wheel does not consume the top-level `subprojects/frida-core` tree directly; it builds against its own fallback copy at `subprojects/frida-python/subprojects/frida-core`. Changing only the top-level tree is therefore not enough to migrate the installed `_frida.abi3.so`.
- p1: An attempted full host-side synchronization of that private `frida-core` subtree progressed further than before but is not yet clean. The host wheel can now be rebuilt far enough to emit `re.sunday.*` paths in `_frida.abi3.so`, but the source build still diverges from the currently working package because its linked extension is not yet runtime-clean (`__frida_compiler_backend_build` / compiler-backend naming drift was observed during one attempt). The host Python environment was restored to a working `frida 17.8.0` after that failure so the workstation is not left broken.
- p1: The protocol migration is not yet fully runtime-clean. `frida.get_device_manager().add_remote_device("127.0.0.1:27142")` still reaches the server but fails on process enumeration with `ProtocolError`, and the demo harness no longer fails on the old `AgentSessionProvider` name mismatch but still does not complete injected validation.
- p1: Current demo evidence after the synchronized rename has shifted. `spawn` now fails earlier with `NotSupportedError: need Gadget to attach on jailed Android`, which means the old namespace mismatch is no longer the first failure on that path. `attach` gets through the baseline scan (`attach_baseline_0` remains `0 hit`) but then fails with `ServerNotRunningError: unable to connect to remote frida-server: closed`, so the injected phase still needs follow-up debugging.
- p1: A spawn+attach smoke against `/system/bin/sleep` still fails with `dlopen failed: library "/proc/self/fd/3" not found`, but the official `frida-server` 17.8.0 reproduces the same failure on the same device. Treat it as a current device/runtime parity issue, not a migration regression.
- p1: The historical top-level versioned directory rename is now complete. Any future local-path-sensitive scripts or docs must use `frida17.8.0/`; the macOS vendored toolchain wrappers in the top-level, `frida-core`, and `frida-python` trees have been retargeted to the new absolute prefix, and stale untracked `frida-python` build caches carrying the old root path were removed during the rename cleanup.
- p2: Temporary local build directories used for the host/server rebuild were cleaned up after validation. The device-side `sunda` instance remains running for follow-up runtime debugging, and the repo-local durable changes still include this checkpoint plus `.findings.sqlite`.
