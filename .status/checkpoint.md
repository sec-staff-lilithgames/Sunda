# Checkpoint

- p0: `frida17.4.1/` is now the active working tree for the new route, but its content has been migrated to the Frida `17.8.0` top-level / `releng` / `frida-core` / `frida-gum` line. The historical directory name is intentionally preserved to avoid outer-path churn.
- p0: `frida-glib/` remains a local preserved component. The current build still depends on local GLib priority logic in `releng/env.py` and `releng/meson_configure.py`, including the Android `target-pkgconfig` overlay path and `gio-2.0.pc` handling.
- p0: A local Android ARM64 build from the migrated tree is functional. With `SSL_CERT_FILE=/Users/siberia/Library/Python/3.12/lib/python/site-packages/certifi/cacert.pem` and `ANDROID_NDK_ROOT=/Users/siberia/Library/Android/sdk/ndk/25.2.9519653`, the server binary at `build-android-arm64-server-cert/subprojects/frida-core/server/sunda` builds successfully, starts on the connected Pixel 8a, and passes `frida-ps -U`.
- p1: The current remaining hardening work is on the new 17.8 Android helper/session surface. The migrated binary already exposes `sunda-*` markers such as `sunda-agent-*`, `sunda-helper-*`, `sunda-main-loop`, and `re.sunday.sunda`, but still retains upstream `re.frida.*`, `/data/local/tmp/frida-helper-`, and `/frida-helper-` strings that now live in the 17.8 helper path.
- p1: A spawn+attach smoke against `/system/bin/sleep` still fails with `dlopen failed: library "/proc/self/fd/3" not found`, but the official `frida-server` 17.8.0 reproduces the same failure on the same device. Treat it as a current device/runtime parity issue, not a migration regression.
- p1: The demo baseline contamination finding still stands. A clean “baseline must be zero” claim on the current device remains blocked by pre-existing anonymous `rwxp` pages reporting `frida` hits before explicit attach/spawn.
- p2: Repo hygiene is currently clean. `.findings.sqlite` remains the only reusable findings store and should stay tracked.
