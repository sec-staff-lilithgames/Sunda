# DFS 01

Root: `pairip_init_hwcap_profile` (`0x2A238`)

Init constructor list:
- `0x29FB4` `pairip_init_arch_gate`
- `0x2A238` `pairip_init_hwcap_profile`
- `0x2A66C` `(init_table_blob_A)`
- `0x2A6D0` `(init_table_blob_B)`
- `0x2A734` `pairip_register_atexit_slot_0`
- `0x2A750` `pairip_register_atexit_slot_1`
- `0x2A76C` `pairip_register_atexit_slot_2`

Notes:
- Names in parentheses are still tentative.
- This root is a bounded constructor branch and is suitable as the first DFS entry.
- Current interpretation: platform capability profiling, not direct anti-debug logic.
- This tree has now been fully DFS-expanded to termination.

Full call tree:

```text
pairip_init_hwcap_profile [0x2a238]
├── pairip_build_hwcap_feature_mask [0x2a04c]
├── __system_property_get_plt [0x84680]
├── getauxval_plt [0x84800]
└── strncmp_plt [0x84880]
```

Node interpretation:
- `pairip_init_hwcap_profile`
  Reads `ro.arch`, compares with `exynos9810`, reads auxv values, then feeds them into `pairip_build_hwcap_feature_mask`.
- `pairip_build_hwcap_feature_mask`
  Builds a feature-mask or capability profile from auxv-derived values.
- `__system_property_get_plt`
  Android system property read.
- `getauxval_plt`
  ELF auxv capability read.
- `strncmp_plt`
  String compare used by the architecture gate.

Current confidence:
- `pairip_init_hwcap_profile`: high
- `pairip_build_hwcap_feature_mask`: medium
- `(init_table_blob_A)`, `(init_table_blob_B)`: low-to-medium

Termination proof:
- Root expansion status: `expanded`
- `pairip_build_hwcap_feature_mask`: `leaf_no_callees`
- `__system_property_get_plt`: `import_leaf`
- `getauxval_plt`: `import_leaf`
- `strncmp_plt`: `import_leaf`
- Additional verification: `llvm-objdump` over `0x2A04C..0x2A238` shows no `BL` or `BLR`, so `pairip_build_hwcap_feature_mask` does not call deeper helper functions inside its own body.
